# ============================================================================
# ComfyUI-Docker-Auto —— 根级 .gitignore
# 目标: 仓库只包含三个项目子目录, 其余工作区杂物一律不进 git。
# 采用【默认全忽略 + 白名单放行】: 只要是这三个子目录之外的东西都排除。
# ============================================================================

# 1) 默认忽略根下所有第一级内容
/*

# 2) 白名单: 只放行这三个子目录(以及本文件、根 README/LICENSE 本身)
!/.gitignore
!/README.md
!/LICENSE
!/ComfyUI-docker/
!/ComfyUI-experience/
!/ComfyUI-testkit/

# 3) 全局通用排除(对放行的子目录内也生效; 避免进入版本库)
*.log
log.txt
*.raw
*.7z
*.zip
*.tar.gz
*.safetensors
*.ckpt
*.pt
*.pth
*.onnx
Thumbs.db
.DS_Store
.reasonix/
__pycache__/
*.pyc
